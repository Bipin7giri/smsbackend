export type DATA = {
  to: any;
  sound: string;
  title: string;
  body: string;
  data: any;
};
export type STATUS = {
  status: string;
  id: string;
};

export type NotificationResult = {
  data: STATUS;
};
